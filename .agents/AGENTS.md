# Workspace Rules

## Test-Driven Development (TDD)
- Always use a Test-Driven Development (TDD) approach for this project.
- Before implementing or modifying any function or core logic, you MUST write the corresponding unit tests first.
- Only after the tests are written (and failing) should you write the implementation code to make them pass.
